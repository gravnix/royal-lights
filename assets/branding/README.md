Put your app logo files here.

Recommended:
- `logo.png` (transparent PNG, square, e.g. 1024x1024) - used in app + login
- `logo.svg` (optional) - used only as a fallback
- `app_logo_foreground.png` (optional, for adaptive icons)

